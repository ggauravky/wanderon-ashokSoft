import React, { useEffect, useState } from 'react';
import { Check, Image, RefreshCw, Search } from 'lucide-react';
import { listMediaAssetsApi } from '../../../../services/api';

export default function MediaAssetPicker({ value, onSelect }) {
  const [open, setOpen] = useState(false); const [assets, setAssets] = useState([]); const [query, setQuery] = useState(''); const [search, setSearch] = useState(''); const [page, setPage] = useState(1); const [pagination, setPagination] = useState({ pages: 1 }); const [loading, setLoading] = useState(false); const [error, setError] = useState('');
  useEffect(() => { if (!open) return; setLoading(true); setError(''); listMediaAssetsApi({ type: 'IMAGE', search, page, limit: 18 }).then((data) => { setAssets(data.data || []); setPagination(data.pagination || { pages: 1 }); }).catch((err) => setError(err.message)).finally(() => setLoading(false)); }, [open, search, page]);
  const submit = (event) => { event.preventDefault(); setPage(1); setSearch(query); };
  return <div><button type="button" onClick={() => setOpen(true)} className="inline-flex min-h-10 items-center gap-2 rounded-lg border border-slate-200 px-3 text-sm font-semibold text-slate-700 hover:bg-slate-50"><Image size={16}/>{value ? 'Change approved image' : 'Choose approved image'}</button>{open && <div className="fixed inset-0 z-50 flex items-end justify-center bg-slate-950/50 p-0 sm:items-center sm:p-6" role="dialog" aria-modal="true" aria-label="Choose media asset"><div className="flex max-h-[90vh] w-full max-w-5xl flex-col rounded-t-2xl bg-white shadow-2xl sm:rounded-2xl"><div className="flex items-center justify-between border-b border-slate-200 p-5"><div><h2 className="font-semibold text-slate-950">Approved media</h2><p className="mt-1 text-xs text-slate-500">Active image assets only. This picker does not grant Media Library administration.</p></div><button type="button" onClick={() => setOpen(false)} className="rounded-lg border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-600">Close</button></div><form onSubmit={submit} className="flex gap-2 border-b border-slate-100 p-4"><label className="relative flex-1"><Search size={15} className="absolute left-3 top-3 text-slate-400"/><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search title, destination, or caption" className="min-h-10 w-full rounded-lg border border-slate-200 pl-9 pr-3 text-sm outline-none focus:border-emerald-500"/></label><button className="rounded-lg bg-slate-950 px-4 text-sm font-semibold text-white">Search</button></form><div className="min-h-0 flex-1 overflow-y-auto p-4">{error ? <p className="rounded-lg bg-rose-50 p-4 text-sm text-rose-700">{error}</p> : loading ? <RefreshCw className="mx-auto my-16 animate-spin text-slate-400"/> : assets.length ? <div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-4">{assets.map((asset) => <button type="button" key={asset._id} onClick={() => { onSelect(asset); setOpen(false); }} className="group overflow-hidden rounded-xl border border-slate-200 text-left hover:border-emerald-500"><div className="relative aspect-video bg-slate-100"><img src={asset.storage?.secureUrl} alt={asset.altText || asset.title} className="h-full w-full object-cover"/>{value === asset._id && <span className="absolute right-2 top-2 rounded-full bg-emerald-600 p-1 text-white"><Check size={13}/></span>}</div><div className="p-3"><p className="truncate text-sm font-semibold text-slate-900">{asset.title}</p><p className="truncate text-xs text-slate-500">{asset.geography?.destination || 'Shared media'}</p></div></button>)}</div> : <p className="py-16 text-center text-sm text-slate-500">No approved images match this search.</p>}</div><div className="flex items-center justify-between border-t border-slate-200 p-4 text-sm text-slate-500"><span>Page {pagination.page || page} of {pagination.pages || 1}</span><div className="flex gap-2"><button type="button" disabled={page <= 1} onClick={() => setPage((v) => v - 1)} className="rounded-lg border border-slate-200 px-3 py-2 disabled:opacity-40">Previous</button><button type="button" disabled={page >= (pagination.pages || 1)} onClick={() => setPage((v) => v + 1)} className="rounded-lg border border-slate-200 px-3 py-2 disabled:opacity-40">Next</button></div></div></div></div>}</div>;
}
