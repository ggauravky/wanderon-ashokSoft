import React, { useEffect, useState } from 'react';
import { Loader2, X } from 'lucide-react';

const empty = { creatorUserId: '', amount: '', periodStart: '', periodEnd: '', destination: 'Manual settlement', notes: '' };
const CreatePayoutModal = ({ open, creators, busy, error, onClose, onConfirm }) => {
  const [form, setForm] = useState(empty);
  useEffect(() => { if (open) setForm(empty); }, [open]);
  if (!open) return null;
  const field = (key, value) => setForm((current) => ({ ...current, [key]: value }));
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4"><div role="dialog" aria-modal="true" aria-labelledby="create-payout-title" className="w-full max-w-xl rounded-xl border border-slate-200 bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><div><h2 id="create-payout-title" className="font-semibold text-slate-950">Create payout record</h2><p className="mt-1 text-xs text-slate-500">Records a manual payout workflow; it does not transfer funds.</p></div><button type="button" onClick={onClose} aria-label="Close"><X size={19} /></button></div><form onSubmit={(event) => { event.preventDefault(); onConfirm(form); }} className="space-y-4 p-5"><label className="block text-sm font-medium text-slate-700">Approved creator<select required value={form.creatorUserId} onChange={(event) => field('creatorUserId', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3"><option value="">Select creator</option>{creators.map((creator) => <option key={creator._id} value={creator._id}>{creator.name} · {creator.email}</option>)}</select></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Amount (INR)<input required min="1" step="0.01" type="number" value={form.amount} onChange={(event) => field('amount', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Settlement reference<input value={form.destination} onChange={(event) => field('destination', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Period start<input type="date" value={form.periodStart} onChange={(event) => field('periodStart', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Period end<input type="date" value={form.periodEnd} onChange={(event) => field('periodEnd', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label></div><label className="block text-sm font-medium text-slate-700">Internal notes<textarea rows="3" value={form.notes} onChange={(event) => field('notes', event.target.value)} className="mt-1 w-full rounded-lg border border-slate-200 p-3" /></label>{error && <p className="rounded-lg bg-rose-50 px-3 py-2 text-sm text-rose-700">{error}</p>}<div className="flex justify-end gap-2"><button type="button" onClick={onClose} className="min-h-10 rounded-lg border border-slate-200 px-4 text-sm font-semibold">Cancel</button><button disabled={busy} className="inline-flex min-h-10 items-center gap-2 rounded-lg bg-emerald-600 px-4 text-sm font-semibold text-white disabled:opacity-50">{busy && <Loader2 size={15} className="animate-spin" />}Create payout</button></div></form></div></div>;
};
export default CreatePayoutModal;

