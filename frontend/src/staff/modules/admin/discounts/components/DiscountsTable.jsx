import React from 'react';
import { Edit3, Power, Trash2 } from 'lucide-react';
import { discountDate, discountValidity, discountValue } from '../discountHelpers.js';
import DiscountStatusBadge from './DiscountStatusBadge.jsx';

const DiscountsTable = ({ coupons, busyId, onEdit, onToggle, onDelete }) => <div className="hidden overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm lg:block"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-[11px] uppercase tracking-wider text-slate-500"><tr>{['Code', 'Type', 'Value', 'Validity', 'Usage', 'Status', 'Updated', 'Action'].map((label) => <th key={label} className="px-4 py-3 font-semibold">{label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{coupons.map((coupon) => <tr key={coupon._id}><td className="px-4 py-4 font-mono font-semibold text-slate-900">{coupon.code}</td><td className="px-4 py-4 capitalize text-slate-600">{coupon.discountType === 'flat' ? 'Fixed' : coupon.discountType}</td><td className="px-4 py-4 font-semibold">{discountValue(coupon)}</td><td className="px-4 py-4 text-xs text-slate-600">{discountValidity(coupon)}</td><td className="px-4 py-4 text-slate-600">{coupon.usageCount || coupon.totalRedemptions || 0}{coupon.usageLimit != null ? ` / ${coupon.usageLimit}` : ''}</td><td className="px-4 py-4"><DiscountStatusBadge status={coupon.displayStatus} /></td><td className="px-4 py-4 text-xs text-slate-500">{discountDate(coupon.updatedAt)}</td><td className="px-4 py-4"><div className="flex gap-1"><button disabled={busyId === coupon._id} onClick={() => onEdit(coupon)} title="Edit" className="rounded-lg p-2 text-slate-600 hover:bg-slate-100"><Edit3 size={15} /></button><button disabled={busyId === coupon._id || coupon.displayStatus === 'expired'} onClick={() => onToggle(coupon)} title={coupon.isActive === false ? 'Activate' : 'Deactivate'} className="rounded-lg p-2 text-slate-600 hover:bg-slate-100 disabled:opacity-40"><Power size={15} /></button><button disabled={busyId === coupon._id || Number(coupon.usageCount || coupon.totalRedemptions || 0) > 0} onClick={() => onDelete(coupon)} title="Delete unused coupon" className="rounded-lg p-2 text-rose-600 hover:bg-rose-50 disabled:opacity-30"><Trash2 size={15} /></button></div></td></tr>)}</tbody></table></div>;
export default DiscountsTable;

