import React, { useEffect, useState } from 'react';
import { Loader2, X } from 'lucide-react';
const empty = { code: '', discountType: 'percentage', discountValue: '', minimumAmount: '0', maximumDiscount: '', startsAt: '', endsAt: '', usageLimit: '', isActive: true };
const dateInput = (value) => value ? new Date(value).toISOString().slice(0, 10) : '';
const DiscountFormModal = ({ coupon, open, busy, error, onClose, onConfirm }) => {
  const [form, setForm] = useState(empty);
  useEffect(() => { if (open) setForm(coupon ? { code: coupon.code || '', discountType: coupon.discountType || 'percentage', discountValue: coupon.discountValue ?? '', minimumAmount: coupon.minimumAmount ?? 0, maximumDiscount: coupon.maximumDiscount ?? '', startsAt: dateInput(coupon.startsAt), endsAt: dateInput(coupon.endsAt || coupon.expiryDate), usageLimit: coupon.usageLimit ?? '', isActive: coupon.isActive !== false } : empty); }, [coupon, open]);
  if (!open) return null;
  const field = (key, value) => setForm((current) => ({ ...current, [key]: value }));
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4"><div role="dialog" aria-modal="true" className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b px-5 py-4"><div><h2 className="font-semibold text-slate-950">{coupon ? 'Edit discount' : 'Create discount'}</h2><p className="mt-1 text-xs text-slate-500">Public checkout coupon rules stored in MongoDB.</p></div><button onClick={onClose} type="button"><X size={18} /></button></div><form onSubmit={(event) => { event.preventDefault(); onConfirm(form); }} className="space-y-4 p-5"><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm font-medium text-slate-700 sm:col-span-2">Coupon code<input required value={form.code} onChange={(event) => field('code', event.target.value.toUpperCase())} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3 font-mono uppercase" /></label><label className="text-sm font-medium text-slate-700">Type<select value={form.discountType} onChange={(event) => field('discountType', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3"><option value="percentage">Percentage</option><option value="flat">Fixed amount</option></select></label><label className="text-sm font-medium text-slate-700">Value<input required min="0.01" max={form.discountType === 'percentage' ? '100' : undefined} step="0.01" type="number" value={form.discountValue} onChange={(event) => field('discountValue', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Minimum booking<input min="0" step="0.01" type="number" value={form.minimumAmount} onChange={(event) => field('minimumAmount', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Maximum discount<input min="0" step="0.01" type="number" value={form.maximumDiscount} onChange={(event) => field('maximumDiscount', event.target.value)} placeholder="No cap" className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Starts<input type="date" value={form.startsAt} onChange={(event) => field('startsAt', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Ends<input type="date" value={form.endsAt} onChange={(event) => field('endsAt', event.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label><label className="text-sm font-medium text-slate-700">Usage limit<input min={coupon?.usageCount || 0} type="number" value={form.usageLimit} onChange={(event) => field('usageLimit', event.target.value)} placeholder="Unlimited" className="mt-1 min-h-11 w-full rounded-lg border border-slate-200 px-3" /></label></div><label className="flex items-center gap-3 rounded-lg border border-slate-200 p-3 text-sm font-medium text-slate-700"><input type="checkbox" checked={form.isActive} onChange={(event) => field('isActive', event.target.checked)} className="h-4 w-4 accent-emerald-600" />Active at checkout when all validity rules pass</label>{error && <p className="rounded-lg bg-rose-50 px-3 py-2 text-sm text-rose-700">{error}</p>}<div className="flex justify-end gap-2"><button type="button" onClick={onClose} className="min-h-10 rounded-lg border px-4 text-sm font-semibold">Cancel</button><button disabled={busy} className="inline-flex min-h-10 items-center gap-2 rounded-lg bg-emerald-600 px-4 text-sm font-semibold text-white disabled:opacity-50">{busy && <Loader2 size={15} className="animate-spin" />}Save discount</button></div></form></div></div>;
};
export default DiscountFormModal;

