import React from 'react';
import { Edit3, Power, Trash2 } from 'lucide-react';
import { discountValidity, discountValue } from '../discountHelpers.js';
import DiscountStatusBadge from './DiscountStatusBadge.jsx';
const DiscountCard = ({ coupon, busy, onEdit, onToggle, onDelete }) => <article className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between"><div><p className="font-mono font-semibold text-slate-950">{coupon.code}</p><p className="mt-1 text-sm capitalize text-slate-500">{coupon.discountType} · {discountValue(coupon)}</p></div><DiscountStatusBadge status={coupon.displayStatus} /></div><p className="mt-4 text-xs text-slate-500">Validity</p><p className="mt-1 text-sm text-slate-700">{discountValidity(coupon)}</p><p className="mt-3 text-xs text-slate-500">Usage: {coupon.usageCount || coupon.totalRedemptions || 0}{coupon.usageLimit != null ? ` / ${coupon.usageLimit}` : ''}</p><div className="mt-4 flex gap-2"><button disabled={busy} onClick={() => onEdit(coupon)} className="inline-flex min-h-9 flex-1 items-center justify-center gap-2 rounded-lg border text-sm font-semibold"><Edit3 size={14} />Edit</button><button disabled={busy || coupon.displayStatus === 'expired'} onClick={() => onToggle(coupon)} className="rounded-lg border px-3"><Power size={15} /></button><button disabled={busy || Number(coupon.usageCount || coupon.totalRedemptions || 0) > 0} onClick={() => onDelete(coupon)} className="rounded-lg border border-rose-200 px-3 text-rose-600 disabled:opacity-30"><Trash2 size={15} /></button></div></article>;
export default DiscountCard;

