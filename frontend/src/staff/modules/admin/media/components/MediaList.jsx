import React from 'react';
import { Copy, FileText, Pencil, Search } from 'lucide-react';
import { formatMediaDate, getMediaUrl, humanizeMediaValue } from '../mediaHelpers.js';

const MediaList = ({ assets, onPreview, onEdit, onCopy }) => <div className="overflow-x-auto rounded-xl border border-slate-200 bg-white"><table className="w-full min-w-[920px] text-left"><thead className="border-b border-slate-200 bg-slate-50 text-[11px] font-semibold uppercase tracking-[0.08em] text-slate-500"><tr><th className="px-4 py-3">Asset</th><th className="px-4 py-3">Type</th><th className="px-4 py-3">Destination</th><th className="px-4 py-3">Category</th><th className="px-4 py-3">Source</th><th className="px-4 py-3">Updated</th><th className="px-4 py-3">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{assets.map((asset) => <tr key={asset._id} className="hover:bg-slate-50/70"><td className="px-4 py-3"><div className="flex items-center gap-3">{asset.type === 'IMAGE' ? <img src={getMediaUrl(asset)} alt="" className="h-10 w-14 rounded-md bg-slate-100 object-cover" /> : <span className="flex h-10 w-14 items-center justify-center rounded-md bg-slate-100 text-slate-400"><FileText size={18} /></span>}<div><p className="max-w-64 truncate text-sm font-semibold text-slate-900">{asset.title}</p><p className="max-w-64 truncate text-xs text-slate-500">{asset.altText}</p></div></div></td><td className="px-4 py-3 text-xs text-slate-600">{humanizeMediaValue(asset.type)}</td><td className="px-4 py-3 text-sm text-slate-700">{asset.geography?.destination || '—'}</td><td className="px-4 py-3 text-xs text-slate-600">{(asset.categories || []).join(', ') || '—'}</td><td className="px-4 py-3 text-xs text-slate-600">{humanizeMediaValue(asset.source?.sourceType)}</td><td className="px-4 py-3 text-xs text-slate-500">{formatMediaDate(asset.updatedAt)}</td><td className="px-4 py-3"><div className="flex gap-1"><button type="button" onClick={() => onPreview(asset)} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100" title="Preview"><Search size={15} /></button><button type="button" onClick={() => onEdit(asset)} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100" title="Edit"><Pencil size={15} /></button><button type="button" onClick={() => onCopy(asset)} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100" title="Copy URL"><Copy size={15} /></button></div></td></tr>)}</tbody></table></div>;

export default MediaList;
