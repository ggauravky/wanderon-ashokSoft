import React from 'react';
import { ExternalLink } from 'lucide-react';

const PagePublishingPanel = ({ page, busy, onStatusChange, onSave }) => <aside className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-sm font-semibold text-slate-950">Publishing</h2><p className="mt-2 text-sm text-slate-600">Draft pages remain unavailable on public dynamic routes.</p><div className="mt-4 grid gap-2"><button type="button" onClick={() => onStatusChange('draft')} className={`min-h-10 rounded-lg border px-3 text-sm font-semibold ${page.status === 'draft' ? 'border-amber-300 bg-amber-50 text-amber-900' : 'border-slate-200 text-slate-600'}`}>Draft / unpublished</button><button type="button" onClick={() => onStatusChange('published')} className={`min-h-10 rounded-lg border px-3 text-sm font-semibold ${page.status === 'published' ? 'border-emerald-300 bg-emerald-50 text-emerald-900' : 'border-slate-200 text-slate-600'}`}>Published</button></div><button type="button" disabled={busy} onClick={onSave} className="mt-4 min-h-10 w-full rounded-lg bg-emerald-600 px-4 text-sm font-semibold text-white disabled:opacity-50">{busy ? 'Saving…' : page.status === 'published' ? 'Save and publish' : 'Save draft'}</button>{page._id && page.status === 'published' && <a href={`/page/${page.slug}`} target="_blank" rel="noreferrer" className="mt-2 inline-flex min-h-10 w-full items-center justify-center gap-2 rounded-lg border border-slate-200 text-sm font-semibold text-slate-700"><ExternalLink size={15} />Open public page</a>}</aside>;

export default PagePublishingPanel;

