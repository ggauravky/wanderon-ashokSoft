import { AlertTriangle } from 'lucide-react';
import OperationsStatusBadge from './OperationsStatusBadge.jsx';
import { attentionReasonText, formatOperationsWindow, formatStartTiming } from '../helpers/operationsFormatters.js';

export default function OperationsAttentionList({ departures }) {
  if (!departures.length) return <div className="rounded-xl border border-emerald-200 bg-emerald-50 px-5 py-5 text-sm text-emerald-800">No current departures require attention from the available booking and trip facts.</div>;
  return <div className="grid gap-3 lg:grid-cols-2">{departures.map((departure) => <article key={departure.operationKey} className="rounded-xl border border-slate-200 bg-white p-4"><div className="flex items-start gap-3"><span className={`mt-0.5 flex h-9 w-9 shrink-0 items-center justify-center rounded-lg ${departure.attention.severity === 'HIGH' ? 'bg-rose-50 text-rose-600' : 'bg-amber-50 text-amber-700'}`}><AlertTriangle size={18} strokeWidth={1.8}/></span><div className="min-w-0 flex-1"><div className="flex flex-wrap items-start justify-between gap-2"><div><h3 className="font-semibold text-slate-950">{departure.title}</h3><p className="mt-0.5 text-xs text-slate-500">{departure.destination || 'Destination not recorded'} · {formatStartTiming(departure)}</p></div><OperationsStatusBadge value={departure.attention.severity}/></div><p className="mt-3 text-xs text-slate-500">{formatOperationsWindow(departure)} · {departure.bookingCount} bookings · {departure.travelerCount} travelers</p><div className="mt-3 flex flex-wrap gap-1.5">{departure.attention.reasons.map((reason) => <span key={reason.code} className="rounded-md border border-slate-200 bg-slate-50 px-2 py-1 text-xs font-medium text-slate-700">{attentionReasonText(reason)}</span>)}</div></div></div></article>)}</div>;
}
